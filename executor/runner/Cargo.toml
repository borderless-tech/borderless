[package]
name = "runner"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow.workspace = true
borderless-sdk = { path = "../../sdk/base", features = ["generate_ids"] }
borderless-runtime = { path = "../runtime" }
borderless-kv-store = { path = "../../kv-store" }
clap = { version = "4.5.32", features = ["derive"] }
colog = "1.3.0"
log.workspace = true
